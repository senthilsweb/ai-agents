# Build the wiki (MkDocs Material) and deploy it to GitHub Pages.
# Spec: agents/job-scout/openspec/changes/job-scout-wiki/design.md (D5)
#
# One-time repo setup: Settings -> Pages -> Source = "GitHub Actions".
name: job-scout docs

on:
  push:
    branches: [main]
    paths:
      - "agents/job-scout/docs/**"
      - "mkdocs.yml"
      - ".github/workflows/job-scout-docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: job-scout-docs
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build site (strict — broken links fail)
        # mkdocs pinned <2: the 2.0 rewrite drops the plugin system
        # mkdocs-material 9.x depends on.
        run: |
          pip install --quiet "mkdocs-material==9.*" "mkdocs<2"
          mkdocs build --strict

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
