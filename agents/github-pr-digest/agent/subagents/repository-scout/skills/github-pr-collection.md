---
description: Deterministically collect one repository's pull requests for a time interval.
---

Use the `fetch_pull_requests` tool once. It owns authentication, pagination, filtering, normalization, ordering, and counts. Return its JSON unchanged.
