# PICT Model — 0 (GET /<0>)
# Strength: 2 (pairwise)
# Generated: 2026-06-28T14:54:58.739Z
# Tip: The constraint block is the most valuable part — keep it version-controlled.

role: read_scope, no_scope, anonymous
status: omit, active, inactive, deceased, invalid_value
limit: omit, 1, 20, 100, 101, 0
offset: omit, 0, 50
orgId: omit, valid_org, unknown_org

# Constraints
# IF [role] = "anonymous" → expect HTTP 401 (see constraints in factors_model.json)
# IF [role] = "no_scope" → expect HTTP 403 (see constraints in factors_model.json)
# IF [limit] = "101" → expect HTTP 400 (see constraints in factors_model.json)
# IF [limit] = "0" → expect HTTP 400 (see constraints in factors_model.json)
# IF [status] = "invalid_value" → expect HTTP 400 (see constraints in factors_model.json)

# Must-include rows (≥1 Smoke + ≥1 RBAC-negative per endpoint)
# MUST: [role]="read_scope", [status]="omit", [limit]="20", [offset]="0", [orgId]="valid_org", [_validation_type]="Smoke", [notes]="Happy path default pagination"
# MUST: [role]="anonymous", [status]="omit", [limit]="20", [offset]="0", [orgId]="omit", [_validation_type]="RBAC -ve", [notes]="No token"
# MUST: [role]="no_scope", [status]="omit", [limit]="20", [offset]="0", [orgId]="omit", [_validation_type]="RBAC -ve", [notes]="Wrong scope"
# MUST: [role]="read_scope", [status]="active", [limit]="100", [offset]="0", [orgId]="valid_org", [_validation_type]="Boundary", [notes]="Max limit with status filter"
# MUST: [role]="read_scope", [status]="omit", [limit]="101", [offset]="0", [orgId]="omit", [_validation_type]="Boundary over", [notes]="Limit exceeds maximum"
