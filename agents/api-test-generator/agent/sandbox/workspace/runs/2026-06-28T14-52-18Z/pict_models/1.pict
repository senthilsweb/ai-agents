# PICT Model — 1 (GET /<1>)
# Strength: 2 (pairwise)
# Generated: 2026-06-28T14:54:58.739Z
# Tip: The constraint block is the most valuable part — keep it version-controlled.

role: write_scope, read_scope, no_scope, anonymous
firstName: valid_short, 99_chars, 100_chars, 101_chars, omit
lastName: valid_short, 99_chars, 100_chars, 101_chars, omit
orgId: valid_org, omit
status: omit, active, inactive, deceased
dateOfBirth: omit, valid_past, today, future_date

# Constraints
# IF [role] = "anonymous" → expect HTTP 401 (see constraints in factors_model.json)
# IF [role] = "read_scope" → expect HTTP 403 (see constraints in factors_model.json)
# IF [role] = "no_scope" → expect HTTP 403 (see constraints in factors_model.json)
# IF [firstName] = "omit" → expect HTTP 400 (see constraints in factors_model.json)
# IF [lastName] = "omit" → expect HTTP 400 (see constraints in factors_model.json)
# IF [orgId] = "omit" → expect HTTP 400 (see constraints in factors_model.json)
# IF [firstName] = "101_chars" → expect HTTP 400 (see constraints in factors_model.json)
# IF [lastName] = "101_chars" → expect HTTP 400 (see constraints in factors_model.json)
# IF [dateOfBirth] = "future_date" → expect HTTP 422 (see constraints in factors_model.json)

# Must-include rows (≥1 Smoke + ≥1 RBAC-negative per endpoint)
# MUST: [role]="write_scope", [firstName]="valid_short", [lastName]="valid_short", [orgId]="valid_org", [status]="omit", [dateOfBirth]="omit", [_validation_type]="Smoke", [notes]="Happy path minimal required fields"
# MUST: [role]="write_scope", [firstName]="valid_short", [lastName]="valid_short", [orgId]="valid_org", [status]="active", [dateOfBirth]="valid_past", [_validation_type]="Smoke full", [notes]="All fields provided"
# MUST: [role]="anonymous", [firstName]="valid_short", [lastName]="valid_short", [orgId]="valid_org", [status]="omit", [dateOfBirth]="omit", [_validation_type]="RBAC -ve", [notes]="No token"
# MUST: [role]="read_scope", [firstName]="valid_short", [lastName]="valid_short", [orgId]="valid_org", [status]="omit", [dateOfBirth]="omit", [_validation_type]="RBAC -ve", [notes]="Read scope cannot create"
# MUST: [role]="no_scope", [firstName]="valid_short", [lastName]="valid_short", [orgId]="valid_org", [status]="omit", [dateOfBirth]="omit", [_validation_type]="RBAC -ve", [notes]="No scope"
# MUST: [role]="write_scope", [firstName]="100_chars", [lastName]="100_chars", [orgId]="valid_org", [status]="omit", [dateOfBirth]="omit", [_validation_type]="Boundary", [notes]="At max length — must succeed"
# MUST: [role]="write_scope", [firstName]="101_chars", [lastName]="valid_short", [orgId]="valid_org", [status]="omit", [dateOfBirth]="omit", [_validation_type]="Boundary over", [notes]="firstName over max → 400"
# MUST: [role]="write_scope", [firstName]="valid_short", [lastName]="valid_short", [orgId]="valid_org", [status]="omit", [dateOfBirth]="future_date", [_validation_type]="Semantic error", [notes]="Future DOB → 422"
