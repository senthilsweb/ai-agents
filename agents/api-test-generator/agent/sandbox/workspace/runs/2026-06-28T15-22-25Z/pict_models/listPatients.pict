# PICT Model — listPatients (GET /<listPatients>)
# Strength: 2 (pairwise)
# Generated: 2026-06-28T15:26:18.420Z
# Tip: The constraint block is the most valuable part — keep it version-controlled.

role: read_token, write_token, full_token, no_token, insufficient_scope_token
status: omitted, active, inactive, deceased
limit: null, 1, 20, 99, 100, 101
offset: null, 0, 1, beyond_end
orgId: omitted, valid_org, unknown_org

# Constraints

# Must-include rows (≥1 Smoke + ≥1 RBAC-negative per endpoint)
# MUST: [row]="[object Object]", [_validation_type]="Smoke", [expected_status]="200", [note]="Happy path: authorized read with typical parameters."
# MUST: [row]="[object Object]", [_validation_type]="Functional", [expected_status]="200", [note]="Status filter active: all returned records must have status=active."
# MUST: [row]="[object Object]", [_validation_type]="Functional", [expected_status]="200", [note]="Status filter inactive: all returned records must have status=inactive."
# MUST: [row]="[object Object]", [_validation_type]="Functional", [expected_status]="200", [note]="Status filter deceased: all returned records must have status=deceased."
# MUST: [row]="[object Object]", [_validation_type]="RBAC -ve", [expected_status]="401", [note]="No token: must return 401."
# MUST: [row]="[object Object]", [_validation_type]="RBAC -ve", [expected_status]="403", [note]="write_token lacks read scope: must return 403."
# MUST: [row]="[object Object]", [_validation_type]="RBAC -ve", [expected_status]="403", [note]="Insufficient scope token: must return 403."
# MUST: [row]="[object Object]", [_validation_type]="Boundary", [expected_status]="200", [note]="limit at maximum boundary (100): must return 200 with at most 100 records."
# MUST: [row]="[object Object]", [_validation_type]="Negative", [expected_status]="400", [note]="limit over maximum (101): must return 400."
# MUST: [row]="[object Object]", [_validation_type]="Boundary", [expected_status]="200", [note]="limit at minimum boundary (1): must return at most 1 record."
# MUST: [row]="[object Object]", [_validation_type]="Boundary", [expected_status]="200", [note]="offset beyond total count: data array must be empty."
# MUST: [row]="[object Object]", [_validation_type]="Functional", [expected_status]="200", [note]="Unknown orgId: data array must be empty, total must be 0."
